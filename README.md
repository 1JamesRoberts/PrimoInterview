After cloning this repository

1.Install the required development dependencies (TypeScript, Vitest, and Node types) by running:
  ```bash
  npm install
  ```

2. To execute the code and test, run the following command:
   ```bash
   npm test
   ```

